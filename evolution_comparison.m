function evolution_comparison(sorted_scores, sorted_pcs, means, ep_labels, task_labels, time, subjects_to_load)

% evo_weight_task(sorted_scores, sorted_pcs, ep_labels, task_labels, subjects_to_load);
evo_weight_ep_task(sorted_scores, sorted_pcs, means, ep_labels, task_labels, time, subjects_to_load);


end